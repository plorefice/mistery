(
  axes: {},
  actions: {
    // Movement keys
    Move(N):  [[Key(K)], [Key(Up)]],
    Move(W):  [[Key(H)], [Key(Left)]],
    Move(S):  [[Key(J)], [Key(Down)]],
    Move(E):  [[Key(L)], [Key(Right)]],
    Move(NW): [[Key(Y)]],
    Move(SW): [[Key(B)]],
    Move(SE): [[Key(N)]],
    Move(NE): [[Key(U)]],

    // Action keys
    PickUp:        [[Key(G)]],
    OpenInventory: [[Key(I)]],
    DropItem:      [[Key(D)]],
    Cancel:        [[Key(Escape)]],
  },
)